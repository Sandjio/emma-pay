# Base URL the Expo app uses to call the backend.
# Simulator / web: http://localhost:3000 works as-is.
# Physical device: replace `localhost` with your dev machine's LAN IP (e.g. http://192.168.1.42:3000).
EXPO_PUBLIC_API_URL=http://localhost:3000
